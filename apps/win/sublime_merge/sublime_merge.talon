os: windows
title: /- Sublime Merge/
-


compal [<user.text>]:
    key(ctrl-shift-p)
    insert(user.text or "")

commit:
	key(ctrl-enter)

push:
	key(ctrl-alt-up)

pull:
    key(ctrl-alt-down)

refresh: key(ctrl-shift-h)
